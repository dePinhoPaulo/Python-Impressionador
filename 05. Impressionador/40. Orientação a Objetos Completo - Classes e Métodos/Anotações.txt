AULA_01: Instalando o PyCharm

    Escolhendo IDE;


AULA_02: Como funciona o PyCharm

    Instruções e dicas para usar PyCharm;


AULA_03: Classes e Objetos

    Tudo no python é um objeto;
    Objetos, na verdade, são Classes em python;
    Cada Classe tem Métodos e Atributos;

    Classes:
        Classe = Objeto
            Metdodos = O que aquele Objeto consegue fazer?
            Atributos = Quais são as informações/propriedades do Objeto
        
        Classe = Pessoa
            Metdodos = Andar, Pular, Correr...
            Atributos = Peso, Altura, QI, Tipo Sanguineo...
        
        Classe = TV
            Metdodos = Ligar TV, Mudar volume, canal...
            Atributos = Cor, Canal, Volume, Tamanho da Tela, É SmartTV..
        
    Classes: Exemplos que já usamos
        Classe: String
            Métodos: Replace, Find, capitilize, Format

        Classe: pandas.DataFrame
            Métodos: append, copy, dropna, groupby, filter
            Atributos: iloc, shape, columns, values, loc...

    Cada Objeto é um Tipo. Dizemos: “É um objeto do Tipo String, é um objeto do tipo int, é um Objeto do Tipo DataFrame...”;
    Vamos ver na próxima aula como isso é criado dentro do Python.


AULA_04: O que é uma Classe no nosso código

    Classe é um modelo ou uma planta usada para criar objetos. 
    Ela junta dados (chamados de atributos) e ações (chamadas de métodos) em um único lugar. 
    
    Tudo em Python é baseado nisso: 
        um texto é um objeto de uma classe de texto, e um número é um objeto de uma classe de número.
    
    Componentes de uma Classe:
        Atributos: As variáveis que guardam as características de um objeto;
        Métodos: As funções que definem o que o objeto pode fazer;
        __init__: O método especial usado para iniciar o objeto com seus valores básicos;
        self: A palavra que representa o objeto específico que você está usando no momento.


AULA_05: Conceitos Importantes sobre Programação Orientada a Objetos

    Programação Orientada a Objeto X Programação Estruturada
        Orientada a Objeto (POO):
            Classes:
                Classe TV:
                    Método mudar_canal:
                    Método mudar_voluma:
                    Método ligar_desligar:
            
            Programa:
                minha_TV = tv()
                minha_TV.ligar()
                minha_TV.mudar_canal('Netflix')
                minha_TV.mudar_volume(55)
            
        Programação Estruturada:
            Programa:
                var = 1
                lista = [1,2,3,4,5]

                for item in lista:
                    if item == variavel:
                        print(f'(var) encontrado!')
                print('fim do programa!')
    
    Qual a vantagem da Orientação a Objeto?
        Aproveitar o código sem precisar refazer/copiar tudo
        Encapsulamento -> Proteção a mudanças indesejadas (ex: TV – você não pode desligar a tv no botão de mudar o volume)
        Herança -> Instâncias do Objeto tem as mesmas características, apesar de ter valores diferentes
        Polimorfismo -> Um mesmo método pode ter várias “formas” em diferentes classes (ou subclasses) – ex: Animais -> Gatos x Cachorros


AULA_06: Criando nossa 1ª Classe

    Dentro da classe, você vai criar a "função" (método) __init__
    Esse método é quem define o que acontece quando você cria uma instância da Classe

    Atributos e valores inicis da nossa classe:
        class TV:
        def __init__(self):
            self.cor = 'preta'
            self.ligada = 'delidada'
            self.tamanho = 55
            self.volume = 12
            self.canal = 'Netflix'


AULA_07: O que é o self das Classes

    self é uma convenção para representar a instância atual da classe, ou seja, o objeto específico que você criou e está manipulando no momento.


AULA_08: Criando Métodos dentro das Classes

    "É a function dentro da nossa classe, como ja fizemos, o __init__"

    def mudar_canal(self):
        self.canal = "Disney+"


AULA_09: Parâmetros nos Métodos das Classes

    Passando canal que quero mudar por paramatro:
            def mudar_canal(self, novo_canal):
                self.canal = novo_canal
                print(f'Canal alterado para {novo_canal}')
            
            tv_quarto.mudar_canal('HBO Max')
            print(tv_quarto.canal)
            
        "self sempre sera passado como paramatro na classe"


AULA_10: Parâmetros no init da Classe

    Para uma variavel de classe, sem valor inicial especifico, basta passar como paramatro da função init:
        def __init__(self, tamanho):
            self.cor = 'preta'
            self.ligada = 'desligada'
            self.tamanho = tamanho
            self.volume = 12
            self.canal = 'Netflix'
        
        *Com isso sera obrigado a passar o tamanho da tv para criar.


AULA_11: Atributos de Classe

    Atributos da classe, são para todos, se alterado, será para todas instancias:
        class TV:
            cor = 'preta'

            def __init__(self, tamanho):
                self.ligada = 'desligada'
                self.tamanho = tamanho
                self.volume = 12
                self.canal = 'Netflix'
        
        tv_sala = TV()
        tv_quarto = TV()
        TV.cor = 'branca'


AULA_12: Aplicação - Criando um Sistema de Conta Corrente

    Colocando em prática:
        Nomes de classes inicio de cada palavra letra maiuscula;
        NOme de metodo ou função tudo letra minuscula;

        Construção da classe Conta Corrente:
            class ContaCorrente:
                def __init__(self, nome, cpf):
                    self.nome = nome
                    self.cpf = cpf
                    self.saldo = 0


AULA_13: Criando Métodos para a nossa Classe

    Cada método deve fazer uma função simples;

    Método Consulta de saldo:
        def consultar_saldo(self):
            print('Seu saldo é de: R${:,.2f}'.format(self.saldo))

    Método Depositar dinheiro:
        def depositar(self, valor):
            self.saldo += valor

    Método Sacar dinheiro:
        def sacar(self, valor):
            self.saldo -= valor
    
    Utilizando métodos:
        conta_paulo.depositar(100)
        conta_paulo.sacar(54.5)
        conta_paulo.consultar_saldo()


AULA_14: Métodos Auxiliares Regra de Métodos Simples e Direto ao Ponto

    Podemos utilizar um metodo de nossa classe dentro de outro:
        Utilizando o self, assim como nos atributos:
            self.consultar_saldo()
    
    Método de mimite da conta:
        def limite_conta(self):
            self.limite = -100
            return self.limite
    
    Ajuste do método de saque, que adiciona a verificação do limite da conta:
        def sacar(self, valor):
            if self.saldo - valor < self.limite_conta():
                print("Você não tem saldo sulficiente para sacar esse valor!")
                self.consultar_saldo()
            else:
                self.saldo -= valor


AULA_15: Underline e Métodos Não Públicos

    Métodos Não Públicos:
        Indica um método protegido (uso interno da classe ou subclasses);
        É um aviso para os outros programadores não mexerem.

        def _limite_conta(self):
            self.limite = -100
            return self.limite


AULA_16: Reforço de Organização e Padrões de Classe

    Dicas de identação, quebras de linha na contrução das classes;


AULA_17: Modificando o init de uma Classe já existente

    A criação de um novo atributo na classe vai impactar em todos objetos criados;
    por isso a criação dos atributos deve ser muito bem estruturada;

    adicionando atributos de agencia e conta:
        def __init__(self, nome, cpf, agencia, num_conta):
            self.nome = nome
            self.cpf = cpf
            self.saldo = 0
            self.limite = None
            self.agencia = agencia
            self.num_conta = num_conta


AULA_18: Métodos Estáticos e Modificando Classe da Melhor Forma

    Metdodo estatico:
        Esta na classe, mas não utiliza nenhuma informação dela;
        Esta na classe 100% para apoio.
        Importante sinaliza estes metodos com:
            @staticmethod
        Importante ao Chamar o metodo, colocar o nome da classe antes:
            ContaCorrente._data_hora()
    
    Metdodo de retorno de data e hora (estatico):
        @staticmethod
        def _data_hora():
            fuso_BR = pytz.timezone('Brazil/East')
            horario_BR = datetime.now(fuso_BR)
            return horario_BR.strftime("%d/%m/%y %H:%M:%S")
    
    Metodos de consultar historico de trasações:
        def consultar_transacoes(self):
            print("Histórico de Transações: ")
            print("Valor, Saldo, Data e Hora")
            for transacao in self.transacoes:
                print(transacao)

        "grava as movimentações em uma lista, ex: self.transacoes.append((valor, self.saldo, ContaCorrente._data_hora()))"


AULA_19: Usando objetos e instâncias como Parâmetro

    Utilizando objeto como paramentro para transferir valores?
        def transferir(self, valor, conta_destino):
            self.saldo -= valor
            self.transacoes.append((-valor, self.saldo, ContaCorrente._data_hora()))
            conta_destino.saldo += valor
            conta_destino.transacoes.append((valor, conta_destino.saldo, ContaCorrente._data_hora())) 

        conta_maePaulo = ContaCorrente('Naia', '000.456.000.00', 1, 21450)
        conta_paulo.transferir(50, conta_maePaulo)


AULA_20: Docstring em Classes

    Documentação e intrusção de uso da classe:
        PEP 257: Convenção atual de decstring do python Global;
        
        forma de utilizar:
            """
            Cria um objeto conta corrente para gerenciar as contas dos clientes.
            """
        podeser usada em todos metodos da classe;
        
        para visualizar:
            help(ContaCorrente)


AULA_21: Atributos não Públicos

    Um sublinhado (_):   
        Não permite alteração fora da classe, de forma manual no codigo, somente atraves de um metodo;
        Mas pode ser consultado por fora da classe;
        ex:
            self._cpf = cpf

    Dois sublinhados (__)?
        Não permite alteração fora da classe, de forma manual no codigo, somente atraves de um metodo;
        E não pode ser consultado por fora da classe;
        ex:
            self.__cpf = cpf


AULA_22: Criando outra Classe e Relação entre Classes

    Crinado classe de cartoes de credito, fazendo o relacionamentos entres as classes;
    Que ao criar um cartão em uma conta, ja inserimos o objeto cartão na conta.

        class ContaCorrente:
        def __init__(self, nome, cpf, agencia, num_conta):
            ...
            self._transacoes = []
            self._cartoes = []

        class CartaoCredito():
        def __init__(self, titular, conta_corrente):
            ...
            self.limite = None
            self.conta_corrente = conta_corrente
            conta_corrente._cartoes.append(self)
        
        conta_paulo = ContaCorrente('Paulo', '123.456.789.00', 1, 21448)
        cartao_paulo = CartaoCredito('Paulo', conta_paulo)
        print(cartao_paulo.conta_corrente.num_conta)
        print(conta_paulo._cartoes[0].numero)


AULA_23: Atributos de Data e Aleatórios

    adicionando validade do cartão, numero aleatorio do cartao e codigo de segurança:
        def __init__(self, titular, conta_corrente):
            self.numero = randint(1000000000000000, 9999999999999999)
            self.titular = titular
            self.validade = '{}/{}'.format(CartaoCredito._data_hora().month, CartaoCredito._data_hora().year + 4)
            self.cod_seguranca = '{}{}{}'.format(randint(1,9), randint(1,9), randint(1,9))
            self.limite = 1000
            self.conta_corrente = conta_corrente
            conta_corrente.cartoes.append(self)


AULA_24: Regra do Python - Underline antes dos Atributos

    para alterar atributos não publicos, utilizamos o getter e setter:
        def __init__(self, titular, conta_corrente):
            ...
            self._senha = '1234'
            self.conta_corrente = conta_corrente
            conta_corrente.cartoes.append(self)

        @property
        def senha(self):
            return self._senha

        @senha.setter
        def senha(self, valor):
            if len(valor) == 4 and valor.isnumeric():
                self._senha = valor
            else:
                print("NOVA SENHA INVALIDA!")


AULA_25: Listar Métodos e Atributos de uma Classe

    consultar todos atributos de uma classe:
        print(conta_paulo.__dict__)


AULA_26: Separando Programa e Arquivo de Classes e Importando nossas Classes

    separando arquivos das classes e importando para arquivo do programa:
        from ContasBanco import ContaCorrente, CartaoCredito
        ou
        import ContasBanco
    

AULA_27: Criando uma nova Classe - Agências

    adicionando nova classe de agencia, com seus respctivos metodos:
        def __init__(self, telefone, cnpj, numero):
            self.telefone = telefone
            self.cnpj = cnpj
            self.numero = numero
            self.clientes = []
            self.caixa = 0
            self.emprestimos = []

        def verificar_caixa(self):
            
        def emprestar_dinheiro(self, valor, cpf, juros):
            
        def adicionar_cliente(self, nome, cpf, patrimonio):


AULA_28: Criando SubClasses

    Uma Subclasse herda todos atributos e metodos de sua classe mãe (Agencia):
        'Na declaração da subclasse passo como parrametro o nome da classe mãe'

        class AgenciaVirtual(Agencia):
            pass
        
        agencia_virtual_1 = AgenciaVirtual(22224444, 123456789858, 123)
        agencia_virtual_1.caixa = 15000
        agencia_virtual_1.verificar_caixa()
    

AULA_29: init em SubClasses e Herança

    adicionando metodo init na subclasse:
        'O init da subclasse deve conversar a super classe'
        class AgenciaVirtual(Agencia):
            def __init__(self, site, telefone, cnpj):
                super().__init__(telefone, cnpj, 1000)
                self.site = site
                self.caixa = 1000000
        
        agencia_virtual_1 = AgenciaVirtual('www.agenciavirtual.com', 22224444, 123456789858)

        "Definimos os mesmos atributos de uma agencia normal na virtual, com o diferencial de: Numero de agecia (1000) fixo, valor do caixa na abertura da agencia, e um site."


AULA_30: Finalizando o init nas outras SubClasses

    Finalizando as demais subclasses:
        class AgenciaComum(Agencia):
            def __init__(self, telefone, cnpj):
                super().__init__(telefone, cnpj, numero=randint(1001, 9999))
                self.caixa = 1000000

        class AgenciaPremium(Agencia):
            def __init__(self, telefone, cnpj):
                super().__init__(telefone, cnpj, numero=randint(1001, 9999))
                self.caixa = 10000000
        
        'Herdam todos os metodos e atributos, com o adcional de numero aleatório, sem ser necessario digitar manualmente.'


AULA_31: Métodos Específicos de uma SubClasse

    adcionando métodos especificos de uma subclasse, Agencia Virtual:
        class AgenciaVirtual(Agencia):
            def __init__(self, site, telefone, cnpj):
                super().__init__(telefone, cnpj, 1000)
                ...
                self.caixa_paypal = 0

            def depositar_paypal(self, valor):
                self.caixa -= valor
                self.caixa_paypal += valor
        
            def sacar_paypal(self, valor):
                self.caixa += valor
                self.caixa_paypal -= valor
        
        agencia_virtual_1.depositar_paypal(20000)
        print(agencia_virtual_1.caixa)
        print(agencia_virtual_1.caixa_paypal)
        
        "Métodos de uma subclasse, não podem ser utilizado em outras sub, somente os metodos da superclasse são utilizados em todas."


AULA_32: Polimorfismo - Personalizando um Método em uma SubClasse

    Editando um metodo da superclasse:
        class AgenciaPremium(Agencia):

        def adicionar_cliente(self, nome, cpf, patrimonio):
            if patrimonio > 1000000:
                super().adicionar_cliente(nome, cpf, patrimonio)
            else:
                print("Cliente não tem patrimonio minimo para entrar na Agencia Premium!")
        
        "Pegamos um metodo da classe pai, e aplicamos uma validação no metodo hedado (Polimorfismo)"


AULA_33: Cuidado ao importar Arquivos de Classes

    "Ao excutar nossas casse no arquivo principal (main), pode haver os abejetos que criamos como teste no aruivo da classe."
    Por isso utilizamos essa regra no arquivo da classe, para não expotar os objetos criados ali:
        if __name__ == '__main__':
            agencia_1 = AgenciaComum(32671388, 12345678901)


AULA_34: Mentoria Python Impressionador - Exercitando Classe e Orientação a Objeto

    "Python é orientado a objeto"
    "Tudo no python é um objeto"

    Classe:
        Atributos: Quais as características dessa classe
        Metodos: o que voce consegue fazer com aquilo

    "Toda função que se cria dentro de uma classe, passar o self como parametro, exetos poucas exeções."
    "A grande vantagem: Poder criar vairios objetos com uma classe."

