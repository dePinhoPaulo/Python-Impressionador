AULA_1: Como funciona o BeautifulSoup

    Mais recomendado a paginas estaticas.
    navegação nos dados html da pagina.


AULA_2: Pegando Informações de Páginas HTML

    buscar elementos do sit por tag:
        titulo = site.find('title') 
        print(titulo.text) 
    
    buscar lista de varios elementos:
        links = barra.find_all('a')
        print(links)


AULA_3: Regras para encontrar elemento no BeautifulSoup

    Outras regras de find:
        subtitulo = site.find(class_='tit')
        cabecalho3 = site.find(role='banner', id='header')
        logo = site.find('img', { 'class': "custom-logo lazyloaded", 'data-ll-status': "loaded"})

    Busca por texto:
        foco_mercado = site.find(string='Foco no Mercado')
        
        import re 
        texto = site.find_all(string=re.compile('alunos'))


AULA_4: Parents e Contents

    "navegação para cima e para baixo"

    Parents:
        Mostra os elementos Pai;
        Acima.

    Contents:
        Mostra os elementos Filho;
        Abaixo.


AULA_5: BeautifulSoup em sites reais - Criptomoedas

    Exercico OK:
        Bs4 em site.ipynb


AULA_6: Tratando erros em raspagem de dados

    Tratando erro de AttributeError, presente no codigo da aula passada;


AULA_7: Outras técnicas de raspagem de dados

    raspagem de dados tratando texto extraido da pagina:
        for linha in linhas:
            texto_linha = linha.get_text(separator=';')
            lista_textos = texto_linha.split(';')
            nome = lista_textos[1]
            preco = lista_textos[3]
            print(nome, preco)
    

AULA_8: Exercício - Miniprojeto de BeautifulSoup

    Projeto OK:
        Mini_Projeto.ipynb
